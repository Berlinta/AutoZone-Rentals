
  <%= form_for @booking do |f| %>

    <%= f.label "Notes" %>
    <%= f.text_area :description %>

    <%= f.label "Paid?"%>
    <%= f.check_box :paid %>

    <%= f.hidden_field :car_id, value: params['car_id'] %>

    <%= f.submit %>
  <% end %>